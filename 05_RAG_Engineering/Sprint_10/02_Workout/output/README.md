# Salida generada por los workouts

Los notebooks escriben aquí:

- `chunks.json` / `embeddings.json` — si regeneras el pipeline offline
- `chroma_db/` — índice Chroma (reutilizado entre workouts)

No versionar estos artefactos en git si contienen ejecuciones locales.
