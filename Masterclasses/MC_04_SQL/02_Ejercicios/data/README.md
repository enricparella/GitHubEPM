# Datos — Ejercicios SQL

| Archivo | Contenido |
|---------|-----------|
| `chinook.db` | Base SQLite "Chinook" (tienda de música digital), usada en el ejercicio de JOINs. |
| `Pokemon.csv` | Dataset clásico de los 151 Pokémon de la primera generación (nombre, tipo, estadísticas de combate), usado en el ejercicio de consultas SQL. |

Ambos ficheros van **incluidos** en esta carpeta; no hace falta descargarlos.

⚠️ `Pokemon.csv` no está en UTF-8 (tiene caracteres como ♀/♂ en algunos nombres). Si lo cargas con pandas fuera del notebook, usa `encoding='latin-1'`.
